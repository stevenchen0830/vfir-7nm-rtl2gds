# Reports and manifests are checksummed over LF content - never translate.
* text=auto eol=lf
*.webp -text
*.png -text
# Preserve raw tool-log spacing; source/style checks still apply elsewhere.
*.log -whitespace
*.rpt -whitespace
